[pytest]
# rootdir on sys.path so `src.` and `tests.` import identically whether pytest
# is invoked from the repo root or from an IDE.
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    error
